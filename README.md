This project is to make FPU chip in ASIC. Floating point calulation require a large amount of computation.
So we will make FPU chip, first we define SPEC of FPU chip, write the verilog code according to the SPEC.
